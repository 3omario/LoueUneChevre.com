# DESACTIVE
#
# Trace d'une tentative de deploiement automatise abandonnee.
# Le fichier executable a ete retire avant mise en production : le rollback,
# les secrets, l'environnement de test et la validation post-deploiement
# n'etaient pas clairs.
#
# Notes restantes de Marc :
# - ne pas deployer automatiquement tant qu'un retour arriere n'est pas teste ;
# - ne pas melanger verification, packaging et prod sans etapes lisibles ;
# - garder une validation humaine tant que la saison haute n'est pas passee ;
# - documenter ce qui bloque un changement avant merge.
